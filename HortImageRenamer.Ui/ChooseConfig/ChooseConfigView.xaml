<UserControl x:Class="HortImageRenamer.Ui.ChooseConfig.ChooseConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="White">
    <Grid Margin="9">
      <Grid.RowDefinitions>
          <RowDefinition/>
          <RowDefinition/>
          <RowDefinition/>
      </Grid.RowDefinitions> 
      <TextBlock Text="Choose Configuration:"
                 FontSize="18"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"/>
        <UniformGrid Grid.Row="1"
                     Rows="2">
            <RadioButton x:Name="IsTest">Test</RadioButton>
            <RadioButton x:Name="IsProduction">Production</RadioButton>
        </UniformGrid>
        <TextBlock x:Name="ThumbDir" Text="{Binding Path=ThumbDir, Mode=OneWay}"
                   Grid.Row="2"/>
    </Grid>
</UserControl>
